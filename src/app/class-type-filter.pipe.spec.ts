import { ClassTypeFilterPipe } from './class-type-filter.pipe';

describe('ClassTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
